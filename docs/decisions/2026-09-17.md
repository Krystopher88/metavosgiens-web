# Journal de décisions — 2026-09-17

| Time | Decision / event | Alternatives rejected | Consequences, pitfalls, debt |
|---|---|---|---|
| 10:32 | **Assets de marque supplémentaires, à la demande de Christopher.** Logo en image (PNG clair/sombre/icône seule) et couverture Google Business Profile générés en rendant du vrai HTML/CSS avec la police de marque réelle dans un navigateur (capture d'écran précise), plutôt qu'approximés — aucun outil de rasterisation SVG→PNG ni la police Plus Jakarta Sans n'étaient disponibles localement. En testant le logo large sur la fiche GMB, Christopher a signalé qu'un recadrage carré (format imposé par Google pour la photo de profil) coupait le texte — révélant au passage qu'**aucun favicon de marque n'existait** : `app/favicon.ico` était toujours celui par défaut de `create-next-app`, jamais remplacé. Corrigé par cohérence : icône carrée dédiée (`docs/brand-assets/icone-carree-gmb.png`, fond vert plein, marque centrée avec marge de sécurité pour un recadrage cercle) **et** favicon réel du site (`app/icon.tsx` 32×32 + `app/apple-icon.tsx` 180×180, même pattern `ImageResponse` que `opengraph-image.tsx` déjà existant), `app/favicon.ico` par défaut supprimé pour éviter le conflit. | Garder le logo large existant et juste le recadrer manuellement pour GMB — écarté, le texte du wordmark ne rentre dans aucun carré lisible ; une vraie icône dédiée est le bon outil, pas un recadrage de secours. | Vérifié en navigateur réel (`/icon` et `/apple-icon` rendent bien 32×32 et 180×180), build de prod propre. Reste à déployer sur le VPS (fait juste après cette entrée) et à uploader manuellement la nouvelle icône carrée sur la fiche GMB (hors périmètre de l'agent, Christopher n'a pas donné accès à Google Business Profile). |
| 09:41 | **V1 déclarée terminée par Christopher.** Site en production sur `metavosgiens.com` (Next.js, ancien Symfony coupé et retiré), toutes les phases du `docs/TASKS.md` closes (statique-first, contenu, formulaire de contact réel via Brevo, SEO/GEO, accessibilité/perf, page À propos détaillée, navigation, footer), les deux derniers défauts de production corrigés et déployés. | — (fait, pas une décision entre alternatives) | **Connu, volontairement différé, pas un oubli** : les liens « Voir le cas » des preuves mènent désormais vers le contact plutôt que vers de vraies pages de détail par cas (jamais construites, décision explicite de Christopher de les laisser venir plus tard) ; volumes Docker de l'ancien stack Symfony (dont la base MariaDB) toujours présents sur le VPS, jamais nettoyés. À reprendre en V2 si besoin. |
| 09:38 | **Deux défauts repérés par Christopher sur le site en production, corrigés.** (1) Le libellé de nav « Réalisations » (header + footer) ne correspondait pas au contenu réel de la section `#proof` (« Trois exemples, trois transformations ») — renommé en « Exemples » pour matcher exactement le H2 affiché. (2) Les cartes de `ProofCases` avaient un pseudo-lien « Voir le cas » (`<span>` sans `href` ni `onClick`, juste stylé en vert comme un lien) qui ne menait nulle part — la construction de vraies pages de détail par cas avait été explicitement différée plus tôt dans le projet (pas une régression de cette correction). Remplacé par un vrai lien (`ArrowLink` vers `/#contact`) reformulé honnêtement en « Un besoin similaire ? Parlons-en », fonctionnel dès maintenant sans attendre les pages dédiées. | Construire les pages de détail par cas maintenant pour rendre « Voir le cas » fonctionnel tel quel — écarté, hors périmètre de cette correction et déjà explicitement reporté par Christopher (« on ne va pas faire passer des tasks qui sont censés venir toutes seules »). Supprimer le lien sans le remplacer — écarté, un vrai CTA vers le contact reste utile commercialement, pas juste un correctif cosmétique. | Vérifié en navigateur réel (nouveau serveur de dev lancé sur le port 3010, le 3000 étant occupé par un service tiers sans rapport avec ce projet), build de prod propre. Déployé sur le VPS à 09:40 (`git pull` + `docker compose up -d --build`, aucune coupure — conteneur non partagé avec d'autres apps) et confirmé en production par `curl` sur `metavosgiens.com` : « Réalisations »/« Voir le cas » absents, « Exemples »/« Un besoin similaire ? Parlons-en » présents. |
| 09:35 | **Mise en production sur le VPS, ancien stack Symfony coupé et remplacé par le nouveau Next.js.** Nouveau dépôt GitHub créé (`Krystopher88/metavosgiens-web`, privé) puisque ce projet n'avait aucun remote — l'ancien `Krystopher88/metavosgiens` (Symfony) reste intact comme archive, pas réutilisé/écrasé. Déployé sur le VPS via le même pattern que les autres apps (`/opt/apps/perso/metavosgiens-web`, `docker-compose.prod.yml` rejoignant `proxy_network`). Séquence sans coupure : nouveau conteneur `metavosgiens_nextjs` construit et vérifié en interne (`wget` dans le conteneur) **avant** de toucher au Caddyfile partagé, bascule Caddy ensuite, ancien stack arrêté seulement après vérification que le nouveau servait bien le trafic réel. `.env.prod.local` du VPS créé avec les bonnes clés (`BREVO_API_KEY`/`BREVO_FROM_EMAIL`/`CONTACT_TO_EMAIL`) — Christopher a saisi la vraie clé Brevo lui-même directement sur le VPS, jamais vue ni manipulée par l'agent (vérifiée seulement par longueur de champ, pas en clair). **Piège évité** : `/opt/apps/proxy/Caddyfile` est monté en bind-mount fichier unique dans `proxy_caddy` — un `git pull` change l'inode, donc un reload classique (`caddy reload --config /etc/caddy/Caddyfile`) aurait relu l'ancienne config sans le signaler. Contourné avec `docker exec -i proxy_caddy caddy reload --config /dev/stdin --adapter caddyfile < Caddyfile` (piège déjà documenté par Christopher lui-même dans `vps-proxy/DEPLOY.md`, commit du 2026-06-22 — pas une découverte de l'agent, juste appliqué correctement). Persistance de la config faite ensuite via `docker compose up -d --force-recreate` sur `proxy_caddy` — ~3s de coupure sur tous les sites du VPS (KrystLab, n8n, starter), acceptée explicitement par Christopher car ça sort du périmètre de MetaVosgiens seul. Anciens conteneurs (`metavosgiens_frankenphp`, `messenger_worker`, `gotenberg`, `mercure`, `mariadb`, `redis`, `chromadb`) arrêtés puis supprimés (`docker rm`) sur confirmation explicite de Christopher — il avait déjà une sauvegarde de la base, pas refaite par l'agent. | Pousser le nouveau code dans l'ancien dépôt `metavosgiens` (même nom logique de produit) — écarté, aurait mélangé l'historique Symfony avec un stack totalement différent (Next.js) sans raison technique. Reload Caddy « classique » sans passer par STDIN — aurait silencieusement servi l'ancienne config après le `git pull`, piège déjà documenté. | Volumes Docker des anciens services (dont les données MariaDB) **non supprimés** — seuls les conteneurs l'ont été ; à nettoyer plus tard si confirmé inutile. Vérifié en conditions réelles à chaque étape (`curl` sur le domaine live, pas de simulation) : `metavosgiens.com` (200), `www.metavosgiens.com` (301 → non-www), `/a-propos` (titre correct), `/robots.txt` (règles IA correctes), et les 3 autres sites du VPS toujours up après le redémarrage du proxy partagé. |
