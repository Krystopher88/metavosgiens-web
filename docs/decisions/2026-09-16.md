# Journal de décisions — 2026-09-16

| Time | Decision / event | Alternatives rejected | Consequences, pitfalls, debt |
|---|---|---|---|
| 17:54 | **Section « Notre méthode » (À propos) transformée en vrai flow méthodologique détaillé, à la demande explicite de Christopher.** Contenu : 7 étapes (`components/method-flow.tsx`, `METHOD_FLOW_STEPS`) allant du premier échange à l'accompagnement/évolution — Premier échange, Comprendre la problématique, Expression du besoin, Cahier des charges & proposition, Réalisation, Test & mise en place, Accompagnement & évolution. Grounded dans `docs/PRD.md` (section « Modèle commercial » : échange gratuit → proposition/diagnostic → réalisation → accompagnement) et dans la séquence verbale donnée par Christopher (prise de contact, discussion, problématique, expression de besoin, cahier des charges, « ainsi de suite ») — pas de fait inventé, complété de façon générale pour rester valable quel que soit le type de projet (site, automatisation, outil métier), avec l'aspect collaboratif (décisions communes, informations régulières) intégré dans le texte de chaque étape plutôt qu'en tag séparé. Première implémentation en timeline verticale jugée « trop verticale » par Christopher — refaite en flow horizontal pleine largeur sur desktop (grille 4 colonnes, connecteurs entre nœuds numérotés, tout le contenu visible sans scroll interne à la section), timeline verticale conservée uniquement sur mobile (`md:hidden`/`hidden md:grid`), sur indication explicite que le mode vertical/scrollable reste acceptable en mobile. Inspiré de 3 références visuelles génériques (templates infographie stock) fournies par Christopher — reprises pour la structure (nœuds numérotés reliés par une ligne, disposition horizontale) mais pas le style (pas d'icônes, pas de dégradés, pas de lorem ipsum, palette et typographie du site). | Une seule grille horizontale de 7 colonnes sans retour à la ligne — écarté, texte réel (pas lorem ipsum) illisible dans une colonne aussi étroite sur un écran de 1600px. Un connecteur en courbe façon S (référence 1 de Christopher) — écarté pour l'instant, effort de mise en œuvre (SVG bézier responsive) jugé disproportionné par rapport à une grille avec lignes droites qui remplit déjà l'objectif exprimé (horizontal, pleine largeur, pas de scroll). | **Chevauchement de contenu à surveiller** : cette section détaille maintenant un processus très proche de la section « Comment ça se passe » (bloc navy) plus bas sur la même page, qui couvre le même sujet en 4 étapes plus courtes — pas encore adressé, à soulever avec Christopher avant de retoucher cette section-là. Vérifié en navigateur réel desktop large (flow horizontal, tout visible sans scroll) et mobile (timeline verticale, lignes de connexion intactes), build de prod propre. |
| 17:41 | **Motif `TopographicContours` ajouté à la section « Pour qui » à la demande de Christopher, comme rappel visuel récurrent** — intention explicite de le retrouver dans chaque section de la page à l'avenir, pas juste celle-ci. Positionné `side="right"`, en arrière-plan de la colonne de texte (pas forcément au même endroit que sur l'intro, confirmé acceptable par Christopher). Contrairement aux grilles/promesses (masquées sur mobile), le motif reste visible sur mobile ici — c'est une texture de fond, pas un contenu à part entière, donc pas concerné par la règle de masquage du 17:39. | — (fait, pas une décision entre alternatives) | **Reste à faire, explicitement annoncé par Christopher** : ajouter ce même motif aux autres sections de la page (Notre méthode, Ce que nous faisons concrètement, Comment ça se passe) pour créer un fil visuel récurrent — pas encore fait, à traiter section par section comme le reste de cette passe design. Vérifié en navigateur réel desktop large et mobile, texte toujours lisible, build de prod propre. |
| 17:39 | **Deux ajustements de Christopher en cascade sur la section « Pour qui ».** (1) Une photo d'atelier artisanal proposée pour cette section a été refusée avant génération : Christopher a noté qu'un métier précis (établi, copeaux de bois) exclut visuellement toutes les autres catégories citées (commerces, PME, B2B, structures industrielles) — même piège que l'image d'intro rejetée plus tôt. Remplacé par une première version : les 7 catégories déjà présentes dans le paragraphe, affichées en nuage de mots à taille variable (petit = indépendants, grand = structures industrielles) pour représenter la diversité d'échelle. (2) Cette première version jugée « trop désordonnée » — et provoquait un vrai chevauchement de texte en mobile (rotation + décalage vertical sur des lignes de flex-wrap trop rapprochées). Remplacée par une grille homogène à 2 colonnes avec barre verticale devant chaque catégorie (repris du même principe que les 4 promesses du hero). **Nouvelle règle générale de Christopher, appliquée rétroactivement au hero** : ces représentations visuelles décoratives (promesses du hero, catégories de « Pour qui ») sont masquées sur mobile (`hidden md:flex`/`md:grid`) — seul le texte éditorial (titre + paragraphe) reste visible sur petit écran, plutôt que de forcer leur affichage empilé. | Générer quand même la photo d'atelier — écarté avant même génération, sur signal de Christopher seul (pas de test raté cette fois, correction anticipée). Garder le nuage de mots avec juste le bug mobile corrigé — écarté : le problème signalé n'était pas seulement le bug mobile mais le désordre visuel lui-même en desktop aussi. | Le fichier ne contient plus de logique de taille/rotation variable (nettoyée) — la même règle « masquer les représentations décoratives sur mobile » s'applique désormais aux deux sections concernées, à reprendre si d'autres sections similaires sont ajoutées. Vérifié en navigateur réel desktop large et mobile (plus de chevauchement), build de prod propre. |
| 17:29 | **L'animation au survol (17:24) rejetée par Christopher pour une raison structurelle, pas esthétique** : un visiteur qui ne survole jamais les tags ne voit jamais la représentation — l'info doit être visible immédiatement, sans interaction. `TopographicContours` et le composant `intro-promises.tsx` reviennent à leur état simple (emphasis/hover retirés, code mort supprimé). Nouvelle approche : les 4 promesses deviennent l'élément visuel dominant de la colonne droite elle-même — grand format typographique (26-30px, gras, accent vertical vert), remplaçant les petits tags ET le vide de la colonne droite en un seul geste, motif topographique conservé en texture de fond discrète seulement. | Chercher une 4ᵉ direction imagée (icônes bespoke par promesse, illustration composite) — écarté après 3 tentatives ratées sur ce terrain (photo paysage, image IA avec texte incrusté, animation cachée) : le vrai problème n'était pas le sujet visuel mais le fait de chercher une IMAGE quand le contenu textuel lui-même, mis en forme avec du poids éditorial, suffit — cohérent avec `DESIGN.md` (« Les titres sont grands, denses et éditoriaux »). | Les petits tags/badges de 17:06 disparaissent (remplacés par cette version agrandie, pas dupliqués) pour éviter la redondance visuelle. Vérifié en navigateur réel desktop large et mobile, build de prod propre. |
| 17:24 | **Colonne droite du hero À propos jugée par Christopher « ne représente rien » malgré les tags de promesses appréciés.** Proposition de Christopher : lier une animation aux tags au survol pour représenter concrètement chaque promesse. Implémenté : `TopographicContours` (`components/topographic-contours.tsx`) accepte désormais un `emphasis` optionnel (`focus`/`compact`/`expand`/`shift`) qui module l'échelle, la rotation, l'opacité et — pour « Être visible » seulement — la couleur (vert de marque) des anneaux existants ; nouveau composant client `components/intro-promises.tsx` relie chaque tag survolé (`onMouseEnter`/`onMouseLeave`) à l'état du motif. Mapping sémantique : Être visible → mise en évidence (couleur + légère mise au point), Gagner du temps → contraction, Développer → expansion, Faire évoluer → rotation. `transition-[transform,opacity,border-color]` avec garde `motion-reduce:transition-none` déjà présente sur le composant. | Générer une image par promesse (4 images) — écarté, coût de génération élevé et même risque de dérive cliché que l'image « bureau » déjà rejetée deux fois cette session. Rendre les tags focusables au clavier pour déclencher aussi l'animation — écarté : l'information (le texte des promesses) reste entièrement disponible sans l'animation, qui est un renfort strictement décoratif, donc pas d'obligation d'accessibilité à la déclencher hors survol souris. | Vérifié en navigateur réel : chaque survol testé individuellement (screenshot avant/après pour les 4 états), les 4 transformations sont visuellement distinctes sans ambiguïté. Aucun effet sur mobile/tactile (pas de hover) — comportement par défaut (statique) conservé, pas de régression. Build de prod propre. |
| 17:16 | **Suite de la passe design À propos : deux problèmes distincts remontés par Christopher après avoir vu le rendu réel.** (1) Les sections texte seul (« Pour qui », « Notre méthode ») restaient collées à gauche dans leur section pleine largeur — régression introduite par le passage en sections full-bleed de 17:06, corrigée en centrant ces blocs (`mx-auto`) et en réintroduisant le motif topographique sur « Pour qui » pour combler l'espace. (2) Christopher a généré une image « intro » via IA à partir du seul texte de la section — refusée : texte et icônes incrustés dans l'image (casse l'accessibilité, ne peut plus être modifié en HTML), scène bureau/laptop/mug/pancartes en bois clichée, carte détaillée en arrière-plan (explicitement bannie par `CLAUDE.md`). Christopher a ensuite lui-même rejeté l'idée d'une photo de paysage pour cette section (« on parle directement des entreprises... ça doit représenter ça ») — au lieu d'une nouvelle tentative photo, les 4 promesses (être visible/gagner du temps/développer/faire évoluer, déjà les 4 portes de la home) ont été rendues visuellement explicites sous forme de tags/chips sous le paragraphe d'intro. | Retenter une image générée pour l'intro (photo « métier » ou paysage alternatif) — écarté par Christopher lui-même : le vrai problème n'était pas le sujet de la photo mais le fait qu'une photo n'est pas le bon outil pour ce message précis. Motif abstrait « points/connexions » seul (proposé en alternative, non retenu cette fois — les chips de texte réel répondaient plus directement à la demande). | Page À propos désormais sans aucune photo « conceptuelle » générée par IA, seulement la photo territoriale sur Proximité (justifiée par le contenu) — cohérent avec la discipline `DESIGN.md` de réserver la photo aux cas où elle porte littéralement le message. Vérifié en navigateur réel desktop large (1600px) et mobile (390px), build de prod propre. |
| 17:06 | **Page À propos enrichie : Christopher la trouvait vide comparée à la homepage.** Contenu ajouté depuis une vraie source déjà validée (les « 8 capacités internes » du PRD, jusque-là jamais affichées — reformulées en langage simple, sans jargon, dans une nouvelle section « Ce que nous faisons concrètement »), et mise en page restructurée : motif topographique sur l'intro, grille 2 colonnes pour les 8 capacités, section navy en 4 étapes pour « Comment ça se passe » (au lieu d'un paragraphe), image + texte pour « Proximité ». **Question de fond posée par Christopher en cours de route** : une direction 100% photo de paysage (répétant la famille photo de la homepage) sert-elle vraiment le message de chaque section ? Réponse tranchée section par section plutôt qu'en bloc : la photo n'est gardée QUE sur « Proximité », où le message (ancrage géographique) correspond littéralement au contenu de l'image ; l'intro reste sur le seul motif topographique (déjà le langage visuel principal du territoire selon `DESIGN.md`), une photo y aurait juste répété le beat « on est dans les Vosges » déjà fait par la homepage sans rien dire du positionnement réel de la page (solutions sur mesure). | Mettre une photo sur chaque section à image (proposition initiale de Christopher, « chaque section, je pense ») — retenu comme option mais explicitement écarté après discussion : aurait dilué le rôle de la photo en décoration répétitive plutôt qu'en signal porteur de sens, et demandé 2 générations d'image au lieu d'une. Garder la page 100% texte (statu quo) — aurait laissé le problème signalé par Christopher non résolu. | Une seule image générée et utilisée (`public/images/about-proximite.jpg`, PNG ChatGPT converti en JPG) au lieu des deux initialement prévues — le prompt avait été écrit pour la section intro, réaffecté à Proximité après la discussion (le cadrage au ras du sol convient aux deux contextes). Vérifié en navigateur réel desktop + mobile (390px), build de prod propre. |
| 16:47 | **Bug rapporté par Christopher : l'item de nav « À propos » et le CTA hero « Découvrir notre approche » menaient tous les deux à la section « Proximité » de l'accueil.** Vérifié dans `docs/PRD.md`/`CLAUDE.md` avant de corriger : ce n'était pas voulu — « À propos » est une vraie page dédiée (`/a-propos`), « Proximité » est une section homepage distincte sur la proximité géographique, « notre approche » correspond à la section Méthode (Comprendre/Trouver/Construire). Cause : `LocalSection` gardait `id="about"`, un nom d'ancre resté d'avant que `/a-propos` existe en page à part entière. Corrigé en 3 volets : nav header/footer « À propos » → `/a-propos` (au lieu de `/#about`) ; CTA hero « Découvrir notre approche » → `#method` (nouvel id ajouté sur `Method`) ; `LocalSection` renommée `id="proximite"`. Vérifié en navigateur réel (clic réel + attente réelle, pas de simulation JS) : nav vers `/a-propos` depuis une page tierce, scroll précis vers la section Méthode depuis le hero. | Ne changer que l'un des deux points d'entrée signalés (aurait laissé l'autre confondu avec Proximité). | En testant, `window.scrollTo`/`scrollIntoView` déclenchés via injection JS synchrone (pas un vrai clic + vrai délai) donnaient une lecture `scrollY` bloquée à 128px, laissant croire à un bug de scroll — s'est avéré être un artefact de l'outillage de test (l'animation `scroll-behavior: smooth` ne progresse pas normalement entre des appels `Runtime.evaluate` séparés), pas un défaut du site ; confirmé propre avec un vrai clic + vraie attente + capture d'écran. Le CSS `scroll-behavior: smooth` global (ajouté juste avant ce fix, non journalisé à l'époque) n'a pas non plus été documenté ici formellement — fait. |
| 16:29 | **Bug rapporté par Christopher : le logo du header ne remonte pas en haut depuis la section contact.** Cause : `next/link` ne déclenche aucune navigation (donc aucun scroll) quand la destination est la même route que l'actuelle (`/` → `/`) — introduit par mon propre correctif clavier de la Phase 6 (logo passé en `<Link href="/">`). En creusant, le même bug touchait TOUT lien vers une section de la homepage (`#about`/`#proof`/`#contact`/`#top`) situé dans un composant affiché sur plusieurs pages (header, footer, `ContactCtaButton` réutilisé sur `/a-propos`) : depuis une autre page, ces liens ne naviguaient jamais vers `/`, ils tentaient de scroller la page courante vers un ancre qui n'y existe pas — silencieusement cassé. Corrigé en préfixant ces hrefs par `/` (`/#contact` etc.), exploitant le mécanisme natif de Next.js qui force `window.scrollTo(0,0)`/`scrollIntoView()` sur les ancres `#top`/`#<id>` même en navigation intra-route. Les liens internes à la homepage elle-même (Hero, ProblemDoors, LocalSection, ProblemOverlay) restent en ancre nue — sûrs, jamais rendus ailleurs. | Ne corriger que le logo signalé par Christopher (aurait laissé les mêmes liens cassés dans le header/footer/CTA — juste pas encore remarqués). | Vérifié en navigateur réel : logo depuis la section contact (scroll→0), lien header "Réalisations" depuis `/a-propos` (navigation vers `/` + scroll précis vers `#proof`), lien footer "Contact" depuis `/a-propos` (idem vers `#contact`). |
| 16:22 | **Audit performance approfondi suite au Lighthouse réel de Christopher** (100/100/100/100 desktop, 81/100/100/100 mobile sur `localhost:3002`). Cause du 81 initial (63 avant ça) : il testait le serveur de dev, pas un build de production — confirmé en lui faisant retester sur un build prod local (81 mobile). Creusé plus loin avec `performance_start_trace` sous ralentissement CPU ×4 + réseau Slow 4G (profil mobile par défaut de Lighthouse) : LCP ~850ms (excellent, seuil « bon » à 2500ms), CLS 0.00, DOM à 199 éléments (léger), 688KB de JS non compressé au total, aucun insight de blocage réseau/rendu significatif trouvé. | Continuer à chercher un bug spécifique à corriger (aurait fait perdre du temps sur un problème qui n'existe pas — le score reflète le coût d'hydratation React sous throttling synthétique, pas un défaut du code). | Passer de 81 à 90+ en mobile demanderait de sacrifier de l'interactivité réelle (menu mobile, overlay des portes, formulaire — les 3 seuls Client Components du site, déjà réduits au minimum) plutôt que corriger un bug — compromis produit à ne pas faire sans en discuter avec Christopher. |
| 16:13 | **Phase 6 (Qualité) : audit complet, pas une simple relecture de ce qui existait déjà.** Contraste WCAG AA calculé mathématiquement (pas à l'œil) sur toutes les couleurs de texte du projet : 5 couleurs sous le seuil 4.5:1 trouvées et corrigées (`Eyebrow` sur fond navy notamment — le composant partagé n'avait pas de variante sombre, bug hérité du refactor Phase 1 qui avait copié une couleur déjà insuffisamment contrastée sur ce fond depuis le code d'un agent parallèle). Logo du header trouvé sans lien vers l'accueil pendant l'audit clavier — corrigé. Anneau de focus du footer invisible sur fond navy (couleur par défaut trop proche du fond) — corrigé. Animation de l'overlay des portes ne respectait pas `prefers-reduced-motion` — corrigé. Lighthouse (accessibilité/bonnes pratiques/SEO) : 100/100, confirmant l'audit manuel. | Se fier au fait que le mobile/clavier/contraste avaient déjà été traités en cours de route sans les revérifier formellement (aurait laissé les 3 bugs ci-dessus non détectés). | **Outils de redimensionnement de fenêtre du navigateur (deux implémentations différentes) en panne pendant cette partie de la session** — confirmé en lisant `window.innerWidth` après resize, pas juste en croyant le message de succès de l'outil. Les 4 pages secondaires (à propos, contact, mentions légales, politique de confidentialité) vérifiées par revue de code plutôt qu'en navigateur réel — structure simple (mono-colonne, `max-w-3xl`, aucun breakpoint desktop-only), risque jugé faible mais à revérifier visuellement dès que les outils remarchent. Note transférable déposée dans le vault cross-projet. |
| 15:52 | **Phase 5 (SEO) complétée, avec un volet GEO (Generative Engine Optimization) ajouté à la demande de Christopher.** Recherche web faite avant d'agir (mémoire d'entraînement jugée pas assez fiable sur un sujet qui bouge vite) : `llms.txt` a un effet quasi nul en pratique (97% jamais lus, GPTBot/ClaudeBot/PerplexityBot/Google-Extended crawlent le HTML directement, Google le liste comme "tactique inutile") — ajouté quand même (coût nul) mais sans survendre son utilité à Christopher. Le vrai levier GEO identifié : les données structurées (`FAQPage` notamment), déjà dans le périmètre SEO classique. `robots.txt` autorise explicitement les bots IA (GPTBot, ClaudeBot, PerplexityBot, Google-Extended...), décision confirmée par Christopher (objectif explicite : être mentionné par l'IA). | Miser sur `llms.txt` comme levier principal (aurait donné une fausse impression d'efficacité, contredite par les données 2026). Bloquer les bots IA par défaut (aurait été contraire à l'objectif explicite de Christopher). | Adresse postale complète ajoutée au schema `ProfessionalService` (rue incluse) — d'abord volontairement limitée à ville/code postal/région par prudence, puis complétée à la demande explicite de Christopher : « il faut vraiment avoir une grosse visibilité locale ». Compromis vie privée/visibilité tranché par lui en connaissance de cause. |
| 15:40 | **Formulaire de contact testé en conditions réelles avec Brevo : email reçu en boîte de réception (`contact@krystdev.com`), pas en spam.** Premier essai avec une vraie clé API échoué (401 Unauthorized) — cause réelle trouvée en ajoutant un log d'erreur serveur (`console.error` dans le `catch`, absent jusque-là) : Brevo bloque par défaut les appels API depuis une IP non reconnue (sécurité par IP allowlist), l'IP du serveur de dev (`82.232.189.42`) n'était pas autorisée. Christopher l'a ajoutée dans Brevo → Sécurité → IPs autorisées, second essai réussi. | Laisser le `catch` sans log d'erreur (aurait rendu ce diagnostic impossible sans deviner). | **À refaire pour chaque nouvel environnement de déploiement** : l'IP du VPS de production devra elle aussi être autorisée dans Brevo avant que l'envoi fonctionne en prod — sinon même échec silencieux (401), gérée proprement côté utilisateur mais à anticiper avant la mise en prod plutôt que découverte après coup. |
| 15:30 | **Service d'envoi d'email revu : Resend → Brevo.** Christopher a déjà un compte Brevo (gratuit) et Brevo est une société française — ça règle directement le point RGPD (transfert hors UE) soulevé juste avant à propos de Resend (société américaine), en plus d'éviter de créer un nouveau compte. SDK vérifié via Context7 avant d'écrire le code (`@getbrevo/brevo@6.0.3`, API `BrevoClient`/`client.transactionalEmails.sendTransacEmail`) — la doc trouvée en mémoire correspondait à une version antérieure du SDK (`TransactionalEmailsApi`/`sendTransacEmail` séparé), vérifié contre les vrais fichiers `.d.ts` installés avant d'utiliser l'API. Contenu HTML de l'email échappé (`escapeHtml`) car il inclut du texte saisi par le visiteur. | Garder Resend malgré le compte Brevo existant (aurait demandé de créer un compte en plus et laissé le point RGPD non résolu). | `politique-confidentialite` mise à jour (Brevo au lieu de Resend). `BREVO_API_KEY`/`BREVO_FROM_EMAIL` à renseigner par Christopher — Brevo permet une vérification d'expéditeur simple (OTP par email), sans DNS complet, contrairement à Resend. |
| 15:20 | **Audit des versions de la stack, à la demande de Christopher.** React/React-DOM (19.2.8→19.3.0) et @types/node (20.x→24.13.5, pour matcher le Node.js v24 réellement installé) mis à jour, sans risque. TypeScript (5.9.3→7.0.2) explicitement laissé de côté : TS7 est une réécriture native en Go du compilateur, et `@typescript-eslint` (même en dernière version publiée) ne supporte que `typescript <6.1.0` — bloqué au niveau de l'écosystème, pas une question d'effort. **ESLint 10 tenté puis annulé** : les peer dependencies déclarées semblaient compatibles (vérifié avant d'agir), mais `eslint-config-next@16.3.5` embarque en interne `eslint-plugin-react@7.37.5`, qui appelle `context.getFilename()` — une API supprimée dans ESLint 10. Lint entièrement cassé (erreur fatale), découvert seulement en testant réellement `npx eslint .`, pas en lisant les peer deps. Revenu à ESLint 9.39.5 (techniquement EOL mais fonctionnel). | Forcer ESLint 10 malgré l'échec (aurait cassé le lint typé du projet). Ne pas re-tester après le revert (aurait laissé un doute). | Le blocage ESLint 10 est dans `eslint-config-next` lui-même (dépendance interne non mise à jour), pas dans notre projet — à revoir quand Next.js publiera une version d'`eslint-config-next` compatible. Note transférable déposée dans le vault cross-projet : les peer dependencies déclarées ne suffisent pas à garantir la compatibilité réelle d'un plugin ESLint, il faut tester `eslint .` en conditions réelles. |
| 15:07 | **Formulaire de contact réel (Phase 4) implémenté avec Resend + Zod, via Server Action React 19 (`useActionState`).** Formulaire intégré à la fois dans `ContactCta` (section `#contact` de la homepage) et sur la page dédiée `/contact` — les CTA existants (header, hero, portes) continuent de pointer vers `#contact` sans modification, ils mènent maintenant au vrai formulaire au lieu du `mailto:` placeholder. Bug découvert et corrigé en testant : React réinitialise automatiquement les champs non contrôlés après toute action, y compris en cas d'échec de validation ou d'envoi — les valeurs saisies sont désormais renvoyées par l'action et réinjectées via `defaultValue` pour ne pas faire perdre son message à l'utilisateur. Anti-spam : un champ honeypot (`company_url`), pas de CAPTCHA. | CAPTCHA (rejeté — web/security.md précise explicitement de préférer les honeypots). Formulaire contrôlé (state React par champ) plutôt qu'action non-contrôlée — rejeté, plus de code pour un gain nul ici, le pattern actions/`defaultValue` suffit une fois le bug de reset corrigé. | `RESEND_API_KEY`/`RESEND_FROM_EMAIL` restent vides dans `.env.local`/`.env.example` — l'envoi réel échouera (géré proprement, message d'erreur utilisateur) tant que Christopher n'a pas configuré une clé Resend et vérifié un domaine d'envoi. |
| 14:54 | **Mentions légales et Politique de confidentialité (Phase 3) : réutilisation sélective du contenu en ligne sur metavosgiens.com.** Les mentions légales (identité de l'éditeur, SIRET, adresse, hébergeur IONOS) ont été reprises telles quelles — cette info ne dépend pas du code. La politique de confidentialité en ligne décrit l'ancienne plateforme Symfony (comptes clients, paiements Stripe, assistant IA « Kiara », intégrations Meta, Google Analytics) — hors périmètre V1 explicite du PRD. Réécrite en ne gardant que ce qui s'applique réellement à ce site (formulaire de contact seul, hébergeur IONOS, Resend pour l'envoi d'email, cadre RGPD). Confirmé par Christopher : « utilise ce que tu peux réutiliser... ce qui n'est pas à réutiliser, tu ne le réutilises pas. » | Copier la politique de confidentialité intégralement (aurait publié une politique légalement inexacte, décrivant des traitements de données qui n'existent pas sur ce site). | Contact éditeur repris tel quel (`contact@krystdev.com`), différent du contact commercial du site (`contact@metavosgiens.com`, dans `lib/content.ts`) — écart déjà présent sur le site en production, pas une invention. À surveiller si un vrai formulaire de contact (Phase 4) ou un outil d'analytics est ajouté : la politique de confidentialité devra être mise à jour en conséquence. |
| 13:26 | **Bug corrigé : clic sur le CTA d'un overlay de porte (ex. « Parler de mon projet ») laissait l'overlay visuellement ouvert malgré la navigation vers `#contact`.** Cause racine réelle, trouvée après deux correctifs infructueux : `onCloseAutoFocus` sur `SheetContent` (`components/problem-doors.tsx`) rappelait toujours `lastTriggerRef.current?.focus()` — même quand la fermeture venait d'un clic sur le CTA — et le focus natif du navigateur sur une carte hors du viewport re-scrollait la page en arrière, écrasant le scroll vers Contact. Un correctif intermédiaire (`event.preventDefault()` dans le handler du lien) avait aussi empêché `SheetClose`/`composeEventHandlers` d'appeler `onOpenChange(false)`, empêchant carrément la fermeture. | `SheetClose asChild` + lien natif (1ʳᵉ tentative) : ferme bien mais le scroll natif est tronqué par le verrou de scroll du Sheet. `event.preventDefault()` + `setTimeout` de rattrapage (2ᵉ tentative) : corrige le scroll mais casse la fermeture via `composeEventHandlers`. | Solution finale : abandon de `SheetClose`, callback explicite `onNavigate` remonté de `ProblemOverlay` à `ProblemDoors`, qui ferme le Sheet ET pose un flag (`suppressFocusRestoreRef`) pour que `onCloseAutoFocus` saute la restauration de focus dans ce cas précis (mais la conserve intacte pour Échap/clic extérieur — revérifié empiriquement). Vérifié en navigateur réel sur 2 portes différentes et sur les deux liens de l'overlay. |
| 12:55 | **Bug de gestion du focus trouvé et corrigé dans l'overlay des 4 portes.** L'agent qui a construit `problem-doors.tsx` avait affirmé que le retour de focus au bon bouton (après fermeture au clavier) fonctionnait nativement via shadcn `Sheet`/Radix `Dialog` — vérifié en navigateur réel que c'était faux avec 4 `SheetTrigger` partageant une seule racine `Sheet` : le focus revenait toujours au dernier bouton monté, pas à celui cliqué. | Faire confiance au rapport de l'agent sans vérification navigateur (aurait laissé un vrai défaut d'accessibilité, PRD/DESIGN.md l'exigent explicitement). | Corrigé avec `onCloseAutoFocus` sur `SheetContent` + une ref manuelle vers le dernier bouton cliqué, plutôt que `SheetTrigger`. Note transférable déposée dans le vault cross-projet — piège générique Radix, pas spécifique à ce projet. |
| 12:40 | **Logo final validé : icône « profil de relief » (piste 3, zigzag) devant le mot + trait horizon (piste 2) sous l'ensemble icône+mot, débordant des deux côtés.** Implémenté en code (`components/logo.tsx`), vérifié dans un vrai navigateur. | Mark discret à 3 arcs concentriques (1ʳᵉ itération, jugé trop timide). Trait pleine largeur du conteneur (2ᵉ itération, rejeté — devait suivre la largeur du lockup, pas du bloc parent). | Composant paramétré en `em` pour rester proportionnel à n'importe quelle taille de texte ; deux variantes de couleur (`onLight`/`onDark`) couvrant fond clair et fond navy. |
| 11:35 | **Vague de correctifs de la revue finale appliquée.** Prettier réaligné sur le style réel du dépôt, `README.md` réécrit, convergence des tokens couleur shadcn vers la palette MetaVosgiens, `.dockerignore`/`deploy.sh`/`docker-compose.prod.yml` corrigés — issus de la revue de branche entière (opus), commit `be609d0` + `fc635b6`. | Une deuxième vague de correctifs (le process n'en autorise qu'une). 4 des 8 findings Minor sciemment différés (reliquats d'assets template par défaut dans `public/`, `lib/utils.ts` mort, volume `node_modules` anonyme en dev, pas de `HEALTHCHECK`/`NEXT_TELEMETRY_DISABLED`). | Les 4 différés sont sans urgence (coût faible, souvent absorbés par la Phase 1) mais à ne pas perdre — voir `.superpowers/sdd/2026-09-16-bootstrap-nextjs-docker/progress.md` avant suppression de ce dossier de travail. |
| 11:20 | **Blocage `~/.claude/hooks/config-protection.js` sur `.prettierrc.json` : Christopher applique le changement lui-même**, hors du hook (qui ne surveille que Write/Edit). | Contourner via Bash (aurait défait le garde-fou anti-affaiblissement de config sans son accord — permission laundering). Désactiver le hook temporairement (changement de `~/.claude/settings.json` partagé entre tous ses projets). | Aucune. Le hook reste actif et intact pour tous les projets ; aucune trace de contournement. |
| 10:45 | **Bibliothèque de primitives shadcn/ui : Radix UI.** La CLI shadcn actuelle propose Base UI/Radix UI/React Aria à l'init ; aucun document projet ne tranchait ce point. Décision engageant tous les futurs composants shadcn (Dialog/Sheet pour l'overlay des 4 portes, Form pour le contact), pas seulement le Button de bootstrap. | Base UI (nouveau défaut recommandé par la CLI, plus récent, moins de retour d'expérience). React Aria (accessibilité réputée, mais peu d'exemples shadcn-spécifiques). | Un preset shadcn secondaire (`-p nova`) a aussi été nécessaire pour rendre `init` non-interactif — accepté sans re-consultation, impact nul (toutes ses valeurs sont réécrites par le Step 2 de la Task 3). |
| 10:20 | **Correctif du hook global `~/.claude/hooks/git/commit-quality.js` : retrait de `--format compact` dans son appel ESLint.** ESLint 9 (scaffoldé par `create-next-app`) a retiré ce formatter du cœur ; le hook ne teste que le code de sortie, retirer le flag ne change aucun comportement de détection. | Ajouter `eslint-formatter-compact` en devDependency du seul projet `metavosgiens` (aurait laissé le hook cassé pour tous ses autres projets ESLint 9+). | Note transférable déposée dans `~/.claude/brain/raw/2026-09-16-eslint9-compact-formatter-removed.md` pour le vault cross-projet. |
| 10:05 | **Cible Docker : dev + prod avec un seul Dockerfile multi-stage.** | Docker en dev uniquement, prod déployée nativement sur une plateforme managée (Vercel/Netlify). | Implique la maintenance de stages `dev`/`deps`/`builder`/`runner` distincts dans un seul fichier — fait, voir Task 6 du plan. |
| 10:05 | **Hébergement production : VPS existant de Christopher, réseau Docker externe `proxy_network`, proxy Caddy déjà en place** (pas Traefik, contrairement à l'hypothèse initiale). | Plateforme managée compatible Next.js (Vercel/Netlify). Indécis pour l'instant (Dockerfile prod générique, décision reportée). | Le `Caddyfile` du VPS route encore `metavosgiens.com` vers `metavosgiens_frankenphp:80`, l'ancien projet Symfony abandonné — **à mettre à jour avant le premier déploiement réel, jamais sans accord explicite de Christopher, et seulement via un diff minimal** (fichier partagé avec KrystLab, n8n, starter en prod). |
| 10:05 | **Service d'envoi d'e-mail pour le formulaire de contact : Resend.** | SMTP classique existant (pas de nouveau service tiers, mais identifiants à sécuriser). À définir plus tard (formulaire avec interface interchangeable). | Seules les variables d'environnement sont préparées (`.env.example`) — intégration réelle prévue en Phase 4 du `TASKS.md`, pas dans ce bootstrap. |
| 10:00 | **Les dossiers `Projects/MetaVosgiens/` et `Projects/metavosgiens-sites/` (ancien projet Symfony/FrankenPHP) sont abandonnés.** Confirmé par Christopher ; désactivation prévue seulement quand la nouvelle version (`agenceWeb`, Next.js) sera prête pour la prod. | — (fait, pas une décision entre alternatives) | Ne jamais s'y référer ni y toucher d'ici là. Le `Caddyfile` du VPS pointe encore dessus (voir ligne « Hébergement production » ci-dessus). |
| 09:55 | **Le design visuel passera par Claude Design** (commande `design`, canvas Claude) pour l'exploration homepage/design system en Phase 1/2, en plus des tokens déjà figés dans `DESIGN.md`. | — (fait, pas une décision entre alternatives) | Le code React/Tailwind final sera écrit directement dans le dépôt, informé par le canvas — pas généré automatiquement depuis l'Artifact. |
